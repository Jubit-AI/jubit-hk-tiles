# Pipeline review + harden — 2026-06-14

Multi-agent review (6 dimensions × adversarial verify) of the pipeline after 22 PRs.
14 findings confirmed; triaged below. (Several "confirmed" entries were *positive*
verifications — code already correct — not bugs.)

## Fixed
| Sev | Area | Fix |
|---|---|---|
| 🔴 CRITICAL | `central_render_bake.py render_tile` | Wrapped the whole per-tile body in try/except/finally. Context+page now ALWAYS close; any browser crash / FD exhaustion / nav error is **contained** as a failed tile (returns True) instead of crashing the worker thread (which in concurrent mode would lose its whole chunk + leak resources). |
| 🟠 HIGH | `make_dzi.py` | Input validation: `tile_size >= 1` (was a ZeroDivisionError), `overlap >= 0`, positive image dims. |
| 🟠 HIGH | `central_render_bake.py --elevation` help | Was backwards ("true-iso={EL}" where the default is the SC2000 *dimetric* angle). Corrected. |
| 🟡 MED | `softStylise.js` | Comment said "Floor 0.33" but code is `0.30` (matches spec). Comment fixed. |
| 🟡 LOW | `game-demo/index.html` | Attribution now uses the full legal entity name ("The Government of the Hong Kong SAR (Lands Department)"), matching the viewer. |

## Verified correct (no change)
- ✅ HK Lands Dept / DATA.GOV.HK attribution present + marked REQUIRED in `viewer/index.html`.
- ✅ Stay-on-government-mesh enforced — only the open-data `3dsd` API is used; zero refs to the legacy `hkmapservice` API in code.
- ✅ No API key leak — the HK Lands Dept key appears only in the gitignored `.env.local`; zero occurrences in any tracked file.
- ✅ DZI descriptor uses original full-res dims (spec-correct).
- ✅ The DZI "tiny edge tile" finding was a **false flag** — partial edge/remainder tiles are *correct* standard DZI behaviour (OpenSeaDragon handles them); our test dims are exact 256-multiples regardless.

## Deferred (tracked, not blocking)
- Server **health check** during render (Vite crash currently surfaces only as per-tile timeouts) — MED robustness; add if territory batches hit it.
- Worker-**id logging** in the concurrent path — LOW debuggability.
- `make_dzi` RGBA-always conversion — LOW memory optimization (our inputs are RGB/RGBA anyway).
- **Pre-commercial: confirm the 3dsd open-data terms in writing with Lands Dept** before any public launch (`docs/legal/attribution.md` §licensing). Compliance task with no code impact — **must be done before the dseek.ai production deploy / jubuddy-HK launch**, not before.
